---
import { siteConfig } from "@/config/site";

interface Props {
  items: Array<{
    title: string;
    url: string;
    position: number;
  }>;
  name: string;
  description: string;
  url?: string;
}

const { items, name, description, url = `${siteConfig.url}/blog` } = Astro.props;
---

<script
  is:inline
  type="application/ld+json"
  set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "CollectionPage",
    name,
    description,
    url,
    inLanguage: "en-US",
    isPartOf: {
      "@id": `${siteConfig.url}/#website`,
    },
    mainEntity: {
      "@type": "ItemList",
      numberOfItems: items.length,
      itemListElement: items.map((item) => ({
        "@type": "ListItem",
        position: item.position,
        url: item.url,
        name: item.title,
      })),
    },
  })}
/>
